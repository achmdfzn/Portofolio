## Phase 3: Content & Enhancement — Implementation Plan

### Prioritas (high to low):

---

### 1. Project Detail Pages (`/projects/[slug]`)

**Why high impact:** Recruiter bisa lihat case study lengkap, bukan cuma card.

**Changes:**
- `constants/index.ts` — tambah field `slug: string`, `longDescription: string`, `features: string[]`, `techDetails: string` ke `Project` interface + data (4 proyek)
- `app/projects/[slug]/page.tsx` — baru. Dynamic route, lookup project by slug, render detail layout (hero, description, tech stack, features, links). `generateStaticParams` + `generateMetadata` untuk SEO.
- `components/organisms/project-detail.tsx` — baru. Detail component (bisa reusable)
- `components/organisms/projects-section.tsx` — ubah `<a href="#">` jadi `<Link href={/projects/${project.slug}}>` untuk navigasi ke detail. Pindahkan Source Code link tetap external.

**Files touched:** 2 create, 1 modify

---

### 2. Contact Form (Server Action + Resend)

**Why:** Biar orang bisa kirim pesan langsung.

**Changes:**
- `npm install resend zod` — dependencies
- `app/actions/send-contact.ts` — baru. Server Action dengan validasi Zod, kirim via Resend API
- `components/atoms/input.tsx` — baru. Reusable input component
- `components/atoms/textarea.tsx` — baru. Reusable textarea
- `components/organisms/contact-section.tsx` — tambah form di atas social links. Name, Email, Message fields dengan `useActionState` (React 19). Status feedback (success/error).

**No API route needed** — Server Action lebih idiomatis Next.js 16.

**Catatan:** User perlu set `RESEND_API_KEY` di Vercel env vars sendiri.

**Files touched:** 3 create, 1 modify

---

### 3. Performance & A11y Quick Wins

**Why:** Gap paling kecil dengan impact besar.

**Changes:**
- `next.config.ts` — tambah `images.formats: ['image/avif', 'image/webp']`, `experimental.optimizePackageImports: ['framer-motion']`
- `app/layout.tsx` — tambah skip-to-content link sebagai first focusable element
- `components/organisms/navbar.tsx` — tambah `aria-label="Scroll to top"` di logo AF button. Tambah `aria-current="page"` di nav items aktif. Tambah focus trap + Escape key di mobile menu.
- `app/globals.css` — scope `body *` transition cuma ke `.theme-toggle-btn, .social-link, nav button` — stop universal repaint

**Files touched:** 3 modify

---

### 4. Certificates Showcase (Simple Section)

**Why:** Nambah kredibilitas, effort kecil.

**Changes:**
- `constants/index.ts` — tambah `CERTIFICATES` array (interface: title, issuer, date, href?)
- `components/organisms/certificates-section.tsx` — baru. Grid cards sederhana
- `app/home-page.tsx` — tambah `<AnimateIn delay={0.35}><CertificatesSection /></AnimateIn>` setelah Experience
- `components/organisms/navbar.tsx` — tambah 'Certificates' ke NAV_ITEMS

**Files touched:** 1 create, 2 modify

---

### Summary

| Task | Files | Impact |
|------|-------|--------|
| Project detail pages | 2 create, 1 modify | 🔥 High — portfolio backbone |
| Contact form | 3 create, 1 modify | 🔥 High — conversion |
| Performance & a11y | 3 modify | Medium — polish |
| Certificates | 1 create, 2 modify | Medium — credibility |

Total: **6 create, 7 modify** — no breaking changes.
